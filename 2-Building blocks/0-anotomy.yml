AWSTemplateFormatVersion: "2010-09-09"

Description:
  String

Metadata:
  metadata section

Parameters:
  set of parameters

Mappings:
  set of mappings

Conditions:
  set of conditions

Resources:
  set of resources
  # Mandatory section for the cloudformation

Outputs:
  set of outputs
